# Sanity CMS
VITE_SANITY_PROJECT_ID=your_project_id
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
VITE_SANITY_TOKEN=           # optional: only needed for draft content

# Contact (also managed in src/config/contact.js)
# These are here for reference — the contact.js config file is the source of truth
